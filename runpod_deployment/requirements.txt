# DeepSeek-OCR official tested versions (Python 3.12.9 + CUDA 11.8)
transformers==4.46.3
tokenizers==0.20.3
accelerate>=0.26.0

# FastAPI server
fastapi>=0.121.0
uvicorn>=0.38.0

# Image processing
Pillow>=9.0.0
matplotlib>=3.0.0

# Standard
pydantic>=2.0.0

# CRITICAL: Flash Attention must be installed for fast inference
# Install with: pip install flash-attn==2.7.3 --no-build-isolation
# Without this, inference will be 2-5x slower and may timeout

