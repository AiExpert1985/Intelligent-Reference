# RunPod Pods (Server) Requirements for DeepSeek OCR API
# Install order: PyTorch first, then other dependencies

# PyTorch with CUDA 11.8
# Install separately with: pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu118
# torch==2.6.0
# torchvision==0.21.0

# Transformers for DeepSeek model
transformers>=4.51.1

# Flash Attention for efficient inference
# Install with: pip install flash-attn==2.7.3 --no-build-isolation
# flash-attn==2.7.3

# FastAPI server
fastapi>=0.121.0
uvicorn>=0.38.0
python-multipart

# Image processing
Pillow>=9.0.0

# Hugging Face transfer for faster model downloads
hf_transfer>=0.1.9

# Python standard library (usually included)
pydantic>=2.0.0
